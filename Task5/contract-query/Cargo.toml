[package]
name = "contract-query"
version = "0.1.0"
edition = "2021"

[dependencies]
ethers = { version = "2.0", features = ["rustls", "legacy"] }
tokio = { version = "1.0", features = ["full"] }
dotenv = "0.15"
serde_json = "1.0"
anyhow = "1.0"